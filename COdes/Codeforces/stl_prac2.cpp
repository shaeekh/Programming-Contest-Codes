#include <iostream>
#include <vector>
#include <iterator>
#include <list>
#include <vector>
#include <string>
#include<iostream>
using namespace std;
int main()
{
    list<int> lis(4,0);
    int a;
    for (int i=0;i<lis.size();i++)
    {
        lis.push_back(a);
    }
    //lis.push_front(43);
    //for (int i=0;i<lis.size();i++)

    //{
        //printf("%d\n", lis[i]);
//        cout<<lis[i]<<endl;
   // }
}
